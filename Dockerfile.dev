FROM node:18-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

ENV NEXT_PUBLIC_MASTER_URL_KEY=${NEXT_PUBLIC_MASTER_URL_KEY}
ENV DESCOPE_CLIENT_ID=${DESCOPE_CLIENT_ID}
ENV DESCOPE_CLIENT_SECRET=${DESCOPE_CLIENT_SECRET}

EXPOSE 3000

CMD ["npm", "run", "dev"]