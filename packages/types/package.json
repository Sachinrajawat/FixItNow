{
  "name": "@fixitnow/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod schemas + TypeScript types for FixItNow web and api.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo \"@fixitnow/types is consumed as source — no build step needed.\""
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
